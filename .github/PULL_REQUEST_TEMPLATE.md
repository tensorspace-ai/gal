**What this changes**

**Why**

**Checklist**
- [ ] `./run-tests.sh` passes
- [ ] If I changed either OT engine, I changed both and regenerated
      `tests/vectors.json`
- [ ] If I added a message type that returns content, it filters by wavelet
      participation and has a test proving a non-participant cannot see it
