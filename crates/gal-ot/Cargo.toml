[package]
name = "gal-ot"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
rust-version.workspace = true
keywords.workspace = true
categories.workspace = true
readme = "README.md"
description = "Operational Transformation engine for rich-text collaborative editing"
# The conformance suite replays a vector file that lives at the repository root
# and is shared with the JavaScript engine's tests, so it cannot travel inside
# the packaged crate. Run it from a checkout.
exclude = ["tests/conformance.rs"]

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
